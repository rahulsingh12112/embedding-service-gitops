apiVersion: v1
kind: Service
metadata:
  name: embedding-service
  namespace: rag-system
spec:
  selector:
    app: embedding-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP   # cluster-internal only (Episode 4 — bahar expose nahi)
